#include <stdio.h>
#include <string.h>

int main()
{

    int t;
    scanf("%d", &t);

    // run test case
    for (int i = 0; i < t; i++)
    {
        char str[100000];
        scanf("%s", &str);
        int length = strlen(str);

        for(int j = 0; j<length; j++){

            
        }
    }
    return 0;
}